#!/usr/bin/env bash
cd "${0%/*}" || exit 1
. ../run-tests

USER=j.doe
ADMIN=false
arguments=(--false)
template() {
    cat <<EOF
The user {{USER}} exists.
{{#ADMIN}}
WRONG - should not be an admin.
{{/ADMIN}}
EOF
}
expected() {
    cat <<EOF
The user j.doe exists.
EOF
}

runTest
