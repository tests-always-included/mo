x="repo"
i=2
repo=( "resque" "hub" "rip" )
quote() {
    echo "'${MO_FUNCTION_ARGS[@]}'"
}
double_quote() {
    echo "\"${MO_FUNCTION_ARGS[@]}\""
}
inter_examp() {
    args=($MO_FUNCTION_ARGS)
    first_t=${args[0]}
    second_t=${args[1]}
    first_v=${args[2]}
    second_v=${args[3]}

    [[ "$first_t" == "true" ]] && echo -n "$first_v "
    [[ "$second_t" == "true" ]] && echo -n "$second_v "
}
exists="hello"
#doesnt="foo"
